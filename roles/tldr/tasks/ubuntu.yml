---
# AIDEV-NOTE: Added become: true to package install; added tags
- name: install system software
  become: true
  package:
      name:
          - python3-dev
          - python3-pip
          - python3-setuptools
          - pipx
  tags: [tldr]

- name: Install tldr python package
  become: true
  community.general.pipx:
      name: tldr
  tags: [tldr]
