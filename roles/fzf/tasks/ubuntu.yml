---
# AIDEV-NOTE: Replaced git-clone + shell script install with ansible.builtin.apt (fzf available in apt)
- name: Install fzf
  become: true
  ansible.builtin.apt:
    name: fzf
    state: present
    update_cache: true
  tags: [fzf]
